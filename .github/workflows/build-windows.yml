name: Build Windows Executable

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write  # Needed for creating releases
      actions: read    # Needed for workflow operations
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        version: "latest"
    
    - name: Set up Python
      run: uv python install 3.11
    
    - name: Install dependencies
      run: uv sync
    
    - name: Install PyInstaller
      run: uv add --dev pyinstaller
    
    - name: Build executable with PyInstaller
      run: |
        uv run pyinstaller --name=EasyOTP --onefile --windowed --icon=NONE easyotp/main.py
    
    - name: Install Windows SDK for signtool
      run: choco install windows-sdk-10.1
    
    - name: Create self-signed certificate
      run: |
        $cert = New-SelfSignedCertificate -Subject "CN=EasyOTP, O=Raymond Lowe, OU=Github: raymondclowe, C=CN, ST=Hong Kong, L=Hong Kong" -CertStoreLocation "Cert:\CurrentUser\My" -KeyExportPolicy Exportable -KeySpec Signature -KeyLength 2048 -KeyAlgorithm RSA -HashAlgorithm SHA256 -NotAfter (Get-Date).AddYears(1)
        $password = ConvertTo-SecureString -String "password123" -Force -AsPlainText
        Export-PfxCertificate -Cert $cert -FilePath cert.pfx -Password $password
    
    - name: Sign the executable
      run: signtool sign /f cert.pfx /p password123 /t http://timestamp.digicert.com dist\EasyOTP.exe
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: EasyOTP-Windows
        path: dist/EasyOTP.exe
    
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/EasyOTP.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
